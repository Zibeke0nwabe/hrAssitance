<!DOCTYPE html>
<html lang="en" class="h-full">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>All Managers | AI HR System</title>

  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.0/css/all.min.css" rel="stylesheet" />
  <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet" />
  <style>
    body {
      font-family: 'Roboto', sans-serif;
    }
  </style>
</head>
<body class="min-h-screen bg-white text-gray-900 leading-relaxed">

  <!-- Top Banner -->
  <div class="bg-black text-white text-center py-2 text-sm">
    <p>It’s GO time for generative AI HR assistance — Try Logo Name AI</p>
  </div>

  <!-- Navbar -->
  <nav class="bg-white px-6 py-4 flex items-center justify-between">
    <div class="flex items-center space-x-6">
      <img src="/images/image1.jpeg" alt="Logo" class="h-10 w-auto" />
      <a href="/admin/register-manager?token=<%= token %>" class="text-gray-700 font-semibold hover:text-purple-800 transition">Register Manager</a>
      <a href="/admin/managers?token=<%= token %>" class="text-purple-800 font-semibold hover:underline">All Managers</a>
      <a href="/admin/reports?token=<%= token %>" class="text-gray-700 font-semibold hover:text-purple-800 transition">Reports</a>
      <a href="/admin/settings?token=<%= token %>" class="text-gray-700 font-semibold hover:text-purple-800 transition">Settings</a>
    </div>
    <div class="flex items-center space-x-4">
      <span class="text-gray-700">Welcome Admin: <%= username %></span>
      <a href="/" class="bg-purple-800 text-white px-4 py-2 rounded-full hover:bg-black transition flex items-center gap-2">
        <i class="fas fa-sign-out-alt"></i> Logout
      </a>
    </div>
  </nav>

  <!-- Main Content -->
  <main class="max-w-3xl mx-auto px-6 py-10 bg-white">

    <!-- Back Link -->
    <div class="mb-6">
      <a href="/admin/home?token=<%= token %>" class="text-purple-800 font-medium hover:underline">
        &larr; Back to Warnings
      </a>
    </div>

    <h1 class="text-3xl font-bold mb-6 text-center text-purple-800">👥 All Managers Created by <%= username %></h1>

    <% if (managers.length === 0) { %>
      <p class="text-gray-600 text-center">No managers have been registered yet.</p>
    <% } else { %>
      <ul class="space-y-3 text-lg">
        <% managers.forEach(m => { %>
          <li class="border-b pb-2 text-gray-800">
            <i class="fas fa-user mr-2 text-purple-700"></i> <%= m.username %>
          </li>
        <% }) %>
      </ul>
    <% } %>
  </main>

  <!-- Footer -->
  <footer class="mt-auto py-6 text-center text-sm text-gray-500 border-t">
    <p>Powered by <strong class="text-gray-700">Esgela Coding Team</strong> — Founder: Onwabe Zibeke</p>
    <p>Email: zibekeonwabe@gmail.com</p>
  </footer>
</body>
</html>